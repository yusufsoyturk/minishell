#include "../minishell.h"

// char	*pars(char *input, char **env) //şu an bi sike yaramıyor
// {
// 	char	*token;

// 	token = token_maker(input);
// 	token = is_exist(input , env); //burda token'ı direkt path'li yapıyor
// 	printf("Token is: %s\n", token);
// 	return (token);	
// }